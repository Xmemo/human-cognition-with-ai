{
  "name": "human-cognition-with-ai-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-content.mjs",
    "verify": "node scripts/verify-content.mjs",
    "check": "astro check",
    "build": "npm run sync && npm run verify && astro check && astro build && node scripts/verify-dist.mjs",
    "dev": "npm run sync && astro dev",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/starlight": "0.41.10",
    "astro": "7.2.9"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "typescript": "5.9.2",
    "wrangler": "4.127.1"
  }
}
